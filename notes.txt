align-content - vertical
justify-content - horizontal
order for changing position of flexbox items
flexbasis is for spacing and can be grown

align-content determines the spacing between lines

align-items determines how the items as a whole are aligned within the container.

block means like a rectangle which can be invisible and

body:after is used for loading images content to reduce image flickers.

always put fonts link before the css links
fixed means object stay in pos when scrolling
abs inside relative will move inside container.